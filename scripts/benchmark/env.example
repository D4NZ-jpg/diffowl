# Copy to env.sh (gitignored) and source before running.
# Any Anthropic-compatible endpoint works; a proxy that fronts a subscription is fine for local runs.
export ANTHROPIC_API_KEY="<key>"
export ANTHROPIC_BASE_URL="https://api.anthropic.com"
export DIFFOWL_CREDENTIALS=env
